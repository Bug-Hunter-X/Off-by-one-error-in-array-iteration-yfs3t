my @array = (1, 2, 3); foreach my $i (0..$#array) { print $array[$i]; } #Correct way to iterate without error.  Alternatively, use a more Perl-idiomatic approach: foreach my $element (@array) { print $element; }